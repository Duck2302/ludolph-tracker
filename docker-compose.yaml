name: ludolph-tracker
services:
  discord-bot:
    image: ludolph-tracker:latest
    restart: unless-stopped
    volumes:
      - ludolph_tracker_data:/app/data/
    env_file:
      - .env

volumes:
  ludolph_tracker_data: